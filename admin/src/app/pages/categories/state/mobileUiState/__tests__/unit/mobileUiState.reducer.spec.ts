import {
  mobileUiStateReducer,
  MobileUiState,
} from "../../mobileUiState.reducer";
import * as mobileUiActions from "../../mobileUiState.actions";
import { initialState } from "../../mobileUiState.reducer";

describe("UNFOLDED / action", () => {
  it("on toggleUnfoldAll action, should toggle all items unfolded", () => {});
});
